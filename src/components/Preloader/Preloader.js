import React from 'react';
import preloader from "../../assets/img/ajax-loader.gif";

let Preloader = (props) => {
    return <img src={preloader} className={'preloader'} alt={'text'}/>
}

export default Preloader;
